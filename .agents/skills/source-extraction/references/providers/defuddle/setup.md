# Defuddle Setup

Defuddle is a CLI for extracting clean Markdown from web pages.

## Install

```bash
npm install -g defuddle
```

## Verify

```bash
defuddle --version
defuddle parse https://example.com --json --md
```

Defuddle does not require project API keys by default.
